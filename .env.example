
# Absolute origin the site is served from. No trailing slash.
SITE_URL=https://insolo.vitorio-0405.workers.dev

# Exactly "true" only when inSolo is a real business with real contact
# details. While false, the site emits noindex and omits LocalBusiness
# structured data.
INDEXABLE=false
